(function () {

    //Elements
    var logo;
    var logo2;
    
    //Variables for Physics and Movements 
    var collider;
    var heroVelocity;
    var touchX;
    var touchY;
    

    //Packages
    var url = ''
    
    //Game
    var config = configuration();
    var game = new Phaser.Game(config);

}());