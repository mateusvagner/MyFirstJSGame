(function () {

    //Elementos
    var logo = null;
    var logo2 = null;
    var config = configuration();

    //Packages
    var url = ''
    
    //Game
    var game = new Phaser.Game(config);
       //Frame Count
    var frameCount = game.getFrame();


}());